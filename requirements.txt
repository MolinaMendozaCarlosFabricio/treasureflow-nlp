# Entorno del proyecto treasureflow_nlp
# Fine-tuning de un modelo BETO/hate-speech en espanol para moderacion
# de contenido multilabel (grosero / amenaza / inapropiado).
#
# Instalacion (CPU):
#   python -m venv .venv
#   .venv\Scripts\activate        (Windows)
#   pip install -r requirements.txt
#
# Instalacion con GPU NVIDIA (recomendado para entrenar, mucho mas rapido):
#   El "pip install -r requirements.txt" de arriba instala el build CPU-only
#   de torch por defecto en Windows. Para usar la GPU, despues de instalar
#   los requirements corre (ajusta cu130 al CUDA que soporte tu driver,
#   revisa con "nvidia-smi"; cu126 es una alternativa mas conservadora):
#     pip uninstall torch -y
#     pip install torch --index-url https://download.pytorch.org/whl/cu130
#   Verificar con:
#     python -c "import torch; print(torch.cuda.is_available())"

# Hugging Face / entrenamiento
transformers>=4.41,<5
datasets>=2.19,<3
accelerate>=1.0
huggingface_hub>=0.23

# ML / datos
torch>=2.2
scikit-learn>=1.3
pandas>=2.0
numpy>=1.26
unidecode>=1.3
matplotlib>=3.8

# Busqueda de hiperparametros
optuna>=3.6

# API (servir el modelo con FastAPI)
fastapi>=0.110
uvicorn[standard]>=0.29

# Testing
pytest>=8.0
httpx>=0.27

# Configuracion del entorno
python-dotenv>=1.0

# Para ejecutar el notebook (ipykernel es suficiente para VS Code / Jupyter
# remoto; se omite el paquete "jupyter"/"jupyterlab" completo porque sus
# assets estaticos de labextensions rompen la instalacion en Windows sin
# soporte de rutas largas habilitado)
ipykernel>=6.29
